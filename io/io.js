const Socket = require('socket.io-mock');

// simulate listen('domain')
// client connect to server : -> on('connect', c => {});
// simulate on('disconnect') -> on('disconnect', c => {})
// simulate emitToPersistent = clear handle, register from db

// test -> lifecycle
